{
  "name": "cm_back",
  "version": "0.0.1",
  "description": "Callmonitor applications backend",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "watch": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test nodemon index.js",
    "build:ui": "rm -rf build && cd ../cm_front/ && npm run build --prod && cp -r build ../cm_back/"
  },
  "author": "Oliver Martikainen",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^6.0.3",
    "nodemon": "^1.19.1"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.1.0",
    "express": "^4.17.1"
  }
}
