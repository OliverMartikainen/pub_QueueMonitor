{
  "name": "cm_back",
  "version": "0.0.10",
  "description": "QueueMonitor applications backend",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "record": "cross-env NODE_ENV=record node index.js",
    "watch": "cross-env NODE_ENV=development nodemon index.js",
    "showcase": "cross-env NODE_ENV=test nodemon index.js",
    "build:ui": "rm -rf build && cd ../pub_QueueMonitor_Frontend/ && npm run build --prod && cp -r build ../pub_QueueMonitor/"
  },
  "author": "Oliver Martikainen",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^6.0.3",
    "eslint": "^6.7.1",
    "nodemon": "^1.19.1"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.1.0",
    "express": "^4.17.1"
  }
}
